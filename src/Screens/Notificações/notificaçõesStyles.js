import { StyleSheet } from "react-native"

const notificaçõesStyles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
        justifyContent: 'center',
        alignItems: 'center',
    },
})

export default notificaçõesStyles;